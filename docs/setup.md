EC2 setup
Docker installation
kind downloaded 
make file executable using chmod +x ./kind
move it to global system path -> sudo mv ./kind /usr/local/bin/
kubectl installed
kind create cluster --name argocd-cluster
Created Kubernetes cluster
